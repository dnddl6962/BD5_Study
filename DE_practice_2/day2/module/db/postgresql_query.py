## POSTGRESQL : postgres database

queries = {
    "point" : "SELECT * FROM point WHERE credate = '{batch_date}'",
    "lecture" : "SELECT * FROM lecture",
    # "lecture01" : "SELECT * FROM lecture01 WHERE credate = '{batch_date}'",
    # "lecture02" :   "SELECT * FROM lecture02",
    # "taeyoung" :   "SELECT * FROM taeyoung"
}    

